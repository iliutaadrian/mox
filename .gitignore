# dependencies
node_modules/

# local data + secrets (never commit)
*.db
config.yaml
.env

# task-observer skill log (not part of the app)
skill-observations/
fetch.log

# sqlite WAL sidecars
*.db-shm
*.db-wal

# local db backups
*.db.bak

# compiled standalone binary
/dist/
